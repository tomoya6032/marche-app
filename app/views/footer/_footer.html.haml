.footer 
  .footer-description 
    .stores-registration
      .h4 ここは出店者登録
      %p ▼マイページ
      = link_to new_host_session_path, class: 'footer-button' do
        ショップ出店
        %br
        ログイン

      = link_to new_seller_session_path, class: 'footer-button' do
        個人出店
        %br
        ログイン

      %p ▼新規会員登録
      
      = link_to new_host_registration_path, class: 'footer-button' do
        ショップ出店
        %br
        ＼新規会員登録／
      = link_to new_seller_registration_path, class: 'footer-button' do
        個人出店
        %br
        ＼新規会員登録／

      %p ▲HOME  
      = link_to root_path, class: 'footer-button' do
        ホームへ
    
    .event-guidance
      .h4 ここは開催案内
    
    .stores-opner
      .h4 ここは出店者案内
    
    .information-container
      .h4 FAQ
      <br>
      <br>
      <br>
      .organizer-container
      .h4 会社概要
      - if administrator_signed_in?
        %p ログイン中: #{current_administrator.email}
        = link_to "ログアウト", destroy_administrator_session_path, method: :delete
      - else
        = link_to "ログイン", new_session_path(:administrator)
        = link_to "サインアップ", new_registration_path(:administrator)

      - if flash.present?
        .flash
          - flash.each do |message_type, message|
            %div{ class: "alert alert-#{message_type}" }= message


.footer-copylight
  %p &copy; 2025 マルシェイベント
