= render 'header/header' # ヘッダーのパーシャルを呼び出す
.sign-up-page
  .sign-up-container
    .sign-up-header
      %h2.sign-up-title さあ、楽しい<br>イベントを始めよう！
      %p.sign-up-subtitle 充実した機能で、もっとあなたの魅力を届けませんか？

    .sign-up-form-wrapper
      = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'sign-up-form' }) do |f|
        = render "devise/shared/error_messages", resource: resource

        .form-group
          = f.label :email, 'メールアドレス'
          = f.email_field :email, autofocus: true, autocomplete: 'email', class: 'form-control', placeholder: 'your@example.com'
          %p.form-text メールアドレスはログインIDとして使用します。

        .form-group
          = f.label :password, 'パスワード'
          - if @minimum_password_length
            %em.form-text (#{@minimum_password_length}文字以上)
          = f.password_field :password, autocomplete: 'new-password', class: 'form-control', placeholder: '半角英数字8文字以上'

        .form-group
          = f.label :password_confirmation, 'パスワード（確認用）'
          = f.password_field :password_confirmation, autocomplete: 'new-password', class: 'form-control', placeholder: 'パスワードをもう一度入力'

        .form-actions
          = f.submit '新規アカウントを作成', class: 'btn btn-primary btn-block'

    .sign-up-footer
      .other-links
        %p 既に出店者アカウントをお持ちですか？
        = link_to new_session_path(resource_name), class: 'btn btn-secondary' do
          %span.link_inner
            %h3 ログインはこちら

      .terms-and-privacy
        %p
          アカウントを作成することで、
          = link_to '利用規約', '#', class: 'link-underline'
          および
          = link_to 'プライバシーポリシー', '#', class: 'link-underline'
          に同意したものとみなされます。


= render 'footer/footer'