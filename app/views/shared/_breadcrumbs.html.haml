- if @breadcrumbs.present?
  %nav.breadcrumbs{ aria: { label: 'パンくずリスト' } }
    .breadcrumb-list
      - @breadcrumbs.each_with_index do |crumb, index|
        .breadcrumb-item
          - if index == @breadcrumbs.size - 1
            %span.breadcrumb-current= crumb[:name]
          - else
            = link_to crumb[:name], normalize_breadcrumb_path(crumb[:path]), class: 'breadcrumb-link'
